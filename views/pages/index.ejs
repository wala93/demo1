<!DOCTYPE html>
<html lang="en">
<%- include('head') %>
<body>
    <header>
        <h1>
           my book collection
        </h1>
    </header>
    <% books.forEach(book=> { %>
   
    <img src="<%= book.image %>" alt="">
            <h3><%= book.title %></h3>
            <!-- <p><%= book.author %></p>
            <p><%= book.isbn %></p>
            <p><%= book.description %></p>
    -->
   <form action="/books/<%= book.id %>" method="GET">

<button type="submit"> view deatails </button>

</form>
   <% }) %>
</body>
</html>